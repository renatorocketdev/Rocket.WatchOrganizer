<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage
    x:Class="Rocket.WatchOrganizer.UI.Popup.Season.PopupAddMultiSeason"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:input="clr-namespace:Plugin.InputKit.Shared.Controls;assembly=Plugin.InputKit"
    xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView">

    <yummy:PancakeView
        Margin="20,0,20,0"
        Padding="0,0,0,0"
        CornerRadius="10"
        HorizontalOptions="Center"
        VerticalOptions="Center">

        <StackLayout BackgroundColor="#FFF">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="15" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="40" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <BoxView
                    Grid.Row="0"
                    Grid.ColumnSpan="2"
                    BackgroundColor="#652CB3" />

                <Label
                    Grid.Row="1"
                    Grid.ColumnSpan="2"
                    FontSize="17"
                    HorizontalTextAlignment="Center"
                    Style="{StaticResource LabelNunitoBold}"
                    Text="Adicionar Múltiplas Temporadas"
                    TextColor="#808080" />

                <input:AdvancedEntry
                    Grid.Row="2"
                    Grid.ColumnSpan="2"
                    Margin="10,5,10,10"
                    AnnotationColor="Accent"
                    BorderColor="#CCC"
                    CornerRadius="5"
                    FontFamily="Nunito-Regular"
                    IsRequired="True"
                    Keyboard="Numeric"
                    MaxLength="50"
                    MinLength="5"
                    Placeholder="Quantidade de Temporadas"
                    PlaceholderColor="#CCC"
                    TextColor="#333"
                    TextFontSize="17" />

                <Button
                    Grid.Row="3"
                    Grid.Column="0"
                    Margin="10,0,10,10"
                    Clicked="Cancel_ButtonAsync"
                    CornerRadius="10"
                    FontSize="12"
                    Style="{StaticResource ButtonTransparent}"
                    Text="Cancelar"
                    TextColor="#4A00E0" />

                <Button
                    Grid.Row="3"
                    Grid.Column="1"
                    Margin="10,0,10,10"
                    BorderColor="Transparent"
                    CornerRadius="10"
                    FontSize="12"
                    Style="{StaticResource ButtonTransparent}"
                    Text="Confirmar"
                    TextColor="#FFF">
                    <Button.Background>
                        <LinearGradientBrush EndPoint="1,0">
                            <GradientStop Offset="0.1" Color="#7c4ed9" />
                            <GradientStop Offset="0.4" Color="#672be0" />
                            <GradientStop Offset="0.8" Color="#5512de" />
                            <GradientStop Offset="1.0" Color="#4A00E0" />
                        </LinearGradientBrush>
                    </Button.Background>
                </Button>
            </Grid>

        </StackLayout>
    </yummy:PancakeView>

</pages:PopupPage>